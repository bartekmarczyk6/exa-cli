---
phase: quick
plan: 2fq
type: execute
wave: 1
depends_on: []
files_modified:
  - src/exa_cli/client.py
  - tests/test_search.py
  - tests/test_contents.py
  - tests/test_client.py
  - tests/test_commands.py
  - tests/test_config.py
  - tests/conftest.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "research create --poll no longer crashes on empty response body"
    - "All CLI commands have tests covering happy path with mocked HTTP"
    - "Error handling (401, 402, 429, 400, 5xx) is tested"
    - "Flag-to-JSON-body mapping is verified for search, find-similar, contents"
    - "Config file precedence (override > env > file) is tested"
  artifacts:
    - path: "src/exa_cli/client.py"
      provides: "Fixed _request that handles empty/non-JSON responses"
    - path: "tests/conftest.py"
      provides: "Shared fixtures (runner, mock client)"
    - path: "tests/test_client.py"
      provides: "Client unit tests including empty body, retries, error handling"
    - path: "tests/test_commands.py"
      provides: "CLI command integration tests with mocked HTTP"
    - path: "tests/test_config.py"
      provides: "Config precedence tests"
  key_links:
    - from: "client.py _request"
      to: "response.json()"
      via: "content-length/status check before parsing"
      pattern: "response\\.text|response\\.content|response\\.json"
---

<objective>
Fix the JSONDecodeError bug in `client.py` when API returns empty body, and create comprehensive pytest tests for all CLI functionality using respx to mock HTTP.

Purpose: Make the CLI robust against empty responses and establish a full test suite.
Output: Bug fix in client.py, comprehensive test files covering all commands/errors/config.
</objective>

<execution_context>
@C:/Users/barto/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/barto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/exa_cli/client.py
@src/exa_cli/main.py
@src/exa_cli/commands/search.py
@src/exa_cli/commands/find_similar.py
@src/exa_cli/commands/contents.py
@src/exa_cli/commands/answer.py
@src/exa_cli/commands/research.py
@src/exa_cli/errors.py
@src/exa_cli/config.py
@src/exa_cli/formatters.py
@tests/test_search.py
@tests/test_contents.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix client.py empty response bug and add client tests</name>
  <files>src/exa_cli/client.py, tests/test_client.py, tests/conftest.py</files>
  <action>
1. Fix `_request` in `src/exa_cli/client.py` line 30: before calling `response.json()`, check if the response has content. Replace the bare `return response.json()` with:
   ```python
   if response.status_code == 204 or not response.content:
       return {}
   return response.json()
   ```
   This handles 204 No Content and any empty body response.

2. Install test dependencies: `pip install respx pytest` (respx works with httpx natively).

3. Create `tests/conftest.py` with:
   - A `runner` fixture returning `CliRunner(mix_stderr=False)`
   - A `mock_api` fixture using `respx.mock(base_url="https://api.exa.ai")` as context manager
   - An `api_key_env` fixture that sets `EXA_API_KEY=test-key` in env (use `monkeypatch`)

4. Create `tests/test_client.py` testing ExaClient directly:
   - `test_post_success`: mock POST /search returning JSON, verify result
   - `test_get_success`: mock GET endpoint returning JSON
   - `test_empty_response_returns_dict`: mock POST returning 204 with empty body, verify returns `{}`
   - `test_retry_on_429`: mock first call 429, second call 200, verify retries and succeeds
   - `test_retry_on_5xx`: mock 500 then 200, verify retry
   - `test_401_raises_abort`: mock 401, verify `click.Abort` raised via `handle_api_error`
   - `test_402_raises_abort`: mock 402
   - `test_400_raises_abort`: mock 400 with JSON `{"error": "bad query", "tag": "VALIDATION"}`

Use `respx` to mock httpx requests. Each test creates `ExaClient("test-key")` directly.
  </action>
  <verify>
    <automated>cd C:/Users/barto/exa-cli && python -m pytest tests/test_client.py -v</automated>
  </verify>
  <done>Client handles empty responses without JSONDecodeError. All client unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive CLI command tests and config tests</name>
  <files>tests/test_commands.py, tests/test_config.py, tests/test_search.py, tests/test_contents.py</files>
  <action>
1. Create `tests/test_commands.py` with CLI integration tests using `CliRunner` and `respx`. For each test, invoke CLI via `runner.invoke(cli, ["--api-key", "test-key", ...])` and mock the corresponding HTTP endpoint with respx.

   **Search command tests:**
   - `test_search_basic`: invoke `search "test"`, mock POST /search returning `{"results": [{"title": "T", "url": "http://x.com", "id": "1"}]}`, verify exit_code 0
   - `test_search_with_domains`: invoke with `--include-domains ex.com,y.com`, capture the request body via respx side_effect or route pattern, assert `includeDomains` is `["ex.com", "y.com"]`
   - `test_search_with_contents_flags`: invoke with `--text --highlights --summary`, assert payload has `contents` with text/highlights/summary keys
   - `test_search_deep_with_schema`: invoke with `--type deep --output-schema '{"type":"object"}'`, assert payload has `outputSchema`

   **Find-similar tests:**
   - `test_find_similar_basic`: invoke `find-similar "http://example.com"`, mock POST /findSimilar, verify success
   - `test_find_similar_with_filters`: invoke with date/domain filters, verify payload mapping

   **Contents tests:**
   - `test_contents_basic`: invoke `contents "http://a.com" "http://b.com"`, mock POST /contents, verify `urls` in payload is `["http://a.com", "http://b.com"]`

   **Answer tests:**
   - `test_answer_basic`: invoke `answer "what is exa"`, mock POST /answer returning `{"answer": "A search engine", "citations": []}`, verify exit_code 0

   **Research tests:**
   - `test_research_create`: invoke `research create "analyze AI"`, mock POST /research/v0/tasks returning `{"id": "task-1", "status": "running"}`, verify success
   - `test_research_create_poll`: invoke with `--poll`, mock POST returning `{"id": "task-1"}` (this tests the fix — empty-ish response), then mock GET /research/v0/tasks/task-1 returning `{"id": "task-1", "status": "completed", "output": "done"}`, verify completes without error
   - `test_research_get`: invoke `research get "task-1"`, mock GET, verify success

   **Error handling tests (pick any command, e.g. search):**
   - `test_no_api_key`: invoke without --api-key and no env, verify "API key required"
   - `test_401_error`: mock 401 response, verify appropriate error message
   - `test_402_error`: mock 402, verify "Out of credits"
   - `test_400_error`: mock 400 with error JSON, verify validation error shown

   For mocking pattern: use `respx.post("https://api.exa.ai/search").mock(return_value=httpx.Response(200, json={...}))`. Use `respx.mock` decorator or context manager per test.

2. Create `tests/test_config.py`:
   - `test_api_key_override_takes_precedence`: call `get_api_key("override")` with env and file set, verify returns "override"
   - `test_env_var_over_config_file`: set env var, create temp config file, verify env wins
   - `test_config_file_fallback`: no env, create temp config with api_key, verify returns it
   - `test_no_key_returns_none`: no env, no file, verify returns None
   Use `monkeypatch` for env vars and `tmp_path` for config file (monkeypatch `config.CONFIG_FILE`).

3. Update existing `tests/test_search.py` and `tests/test_contents.py` — either merge their single test into test_commands.py or leave them as-is (they still pass).
  </action>
  <verify>
    <automated>cd C:/Users/barto/exa-cli && python -m pytest tests/ -v</automated>
  </verify>
  <done>All CLI commands have passing tests. Flag-to-payload mapping verified. Error codes tested. Config precedence tested. research create --poll works without JSONDecodeError.</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/barto/exa-cli && python -m pytest tests/ -v --tb=short
```
All tests pass. No JSONDecodeError on empty responses.
</verification>

<success_criteria>
- `python -m pytest tests/ -v` shows all tests passing
- research create --poll bug is fixed (empty body returns `{}` instead of raising JSONDecodeError)
- Every CLI command (search, find-similar, contents, answer, research create, research get) has at least one happy-path test
- Error codes 400, 401, 402 are tested
- Config precedence (override > env > file > None) is tested
- Flag-to-JSON mapping tested for search (domains, contents flags, deep mode)
</success_criteria>

<output>
After completion, create `.planning/quick/260402-2fq-create-tests-for-all-cli-functionalities/260402-2fq-SUMMARY.md`
</output>
